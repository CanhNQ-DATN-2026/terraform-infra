# ─── Terraform state & lock ───────────────────────────────────────────────────
*.tfstate
*.tfstate.*
.terraform.tfstate.lock.info
terraform.tfstate.backup

# ─── Local working directory (downloaded providers & modules) ─────────────────
.terraform/
.terraform.lock.hcl   # commit this if you want reproducible provider versions

# ─── Variable files that may contain secrets ──────────────────────────────────
# terraform.tfvars contains no secrets (RDS password is managed by AWS SM).
# It is committed so CI uses the same variable values as local.
# *.auto.tfvars files may contain secrets — keep them ignored.
*.auto.tfvars

# The example file is safe to commit (contains no real secrets).
!terraform.tfvars.example

# ─── Plan output files ────────────────────────────────────────────────────────
*.tfplan
tfplan

# ─── OS & editor noise ────────────────────────────────────────────────────────
.DS_Store
*.swp
*.swo
.idea/
.vscode/
