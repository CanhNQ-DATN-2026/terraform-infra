name: Terraform Drift Reconcile

on:
  # GitHub Actions schedules run in UTC.
  # 00:00 Asia/Ho_Chi_Minh = 17:00 UTC on the previous day.
  schedule:
    - cron: "0 17 * * *"
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

concurrency:
  group: terraform-drift-reconcile-${{ github.ref }}
  cancel-in-progress: false

env:
  TF_IN_AUTOMATION: "true"
  TF_INPUT: "false"
  TF_VERSION: "1.9.0"
  AWS_REGION: ${{ vars.AWS_REGION }}

jobs:
  detect-drift:
    name: Detect Drift
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    outputs:
      drift_found: ${{ steps.detect.outputs.drift_found }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}
          terraform_wrapper: false

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ${{ env.AWS_REGION }}
          role-to-assume: ${{ vars.AWS_ROLE_ARN }}
          role-session-name: github-actions-terraform-drift-detect

      - name: Terraform Init
        run: terraform init -input=false -reconfigure

      - name: Detect Drift
        id: detect
        shell: bash
        run: |
          set +e
          terraform plan -input=false -detailed-exitcode -out=tfplan
          exit_code=$?
          set -e

          case "$exit_code" in
            0)
              echo "drift_found=false" >> "$GITHUB_OUTPUT"
              echo "No actionable drift detected. Terraform would not change any AWS resources to match the code on main." >> "$GITHUB_STEP_SUMMARY"
              ;;
            2)
              echo "drift_found=true" >> "$GITHUB_OUTPUT"
              if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
                echo "Actionable drift detected. This manual run will continue to reconcile AWS back to the code on main." >> "$GITHUB_STEP_SUMMARY"
              else
                echo "Actionable drift detected. Scheduled runs only report changes that Terraform would actually correct; start this workflow manually to reconcile AWS back to the code on main." >> "$GITHUB_STEP_SUMMARY"
              fi
              terraform show -no-color tfplan > drift-plan.txt
              ;;
            *)
              echo "Terraform drift detection failed with exit code $exit_code." >> "$GITHUB_STEP_SUMMARY"
              exit "$exit_code"
              ;;
          esac

      - name: Upload Drift Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: drift-plan-report
          path: drift-plan.txt
          if-no-files-found: ignore
          retention-days: 7

      - name: Upload Drift Plan
        if: steps.detect.outputs.drift_found == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: drift-tfplan
          path: tfplan
          if-no-files-found: ignore
          retention-days: 7

  reconcile:
    name: Reconcile To Code
    runs-on: ubuntu-latest
    needs:
      - detect-drift
    if: github.ref == 'refs/heads/main' && needs.detect-drift.outputs.drift_found == 'true' && github.event_name == 'workflow_dispatch'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}
          terraform_wrapper: false

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ${{ env.AWS_REGION }}
          role-to-assume: ${{ vars.AWS_ROLE_ARN }}
          role-session-name: github-actions-terraform-drift-reconcile

      - name: Terraform Init
        run: terraform init -input=false -reconfigure

      - name: Download Drift Plan
        uses: actions/download-artifact@v4
        with:
          name: drift-tfplan
          path: .

      - name: Terraform Apply
        run: terraform apply -input=false -auto-approve tfplan

      - name: Write Summary
        run: echo "Terraform apply completed to converge live AWS resources back to the code on main." >> "$GITHUB_STEP_SUMMARY"
